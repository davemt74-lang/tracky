{
  "name": "tracky",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Camera-based real-world object tracking input for browser games.",
  "scripts": {
    "test": "node --test && node --check app.js && node --check vertical-motion.js",
    "serve": "python -m http.server 8080"
  }
}
