{
  "name": "tracky",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Camera-based real-world object tracking input for browser games.",
  "scripts": {
    "test": "node --test && node --check app.js && node --check vertical-motion.js && node --check participants.js && node --check src/gameplay-core.js && node --check src/participant-core.js && node --check src/participant-store.js && node --check src/identity-engine.js && node --check src/room-tracking-core.js",
    "serve": "python -m http.server 8080"
  }
}
