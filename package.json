{
  "name": "tracky",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Camera-based real-world object tracking input for browser games.",
  "scripts": {
    "test": "node --test && node --check app.js && node --check vertical-motion.js && node --check src/gameplay-core.js",
    "serve": "python -m http.server 8080"
  }
}
