name: Tracky CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate codebase
        run: npm run validate

      - name: Build deploy ZIP
        shell: bash
        run: |
          set -euo pipefail
          rm -rf dist tracky-v1.3-deploy.zip
          mkdir -p dist/tracky-v1.3/src
          cp index.html agent-eyes.js participants.html participants.js participant-voice.js experiments.html tracker.html tracker-experiment.js games.html vertical-motion.html vertical-motion.js styles.css README.md package.json dist/tracky-v1.3/
          cp src/tracker-core.js src/movement-core.js src/gameplay-core.js src/participant-core.js src/participant-store.js src/identity-engine.js src/room-tracking-core.js src/voice-core.js src/voice-engine.js src/room-audio-engine.js src/room-audio-worklet.js src/model-config.js src/perception-core.js src/behavior-core.js src/object-core.js src/scene-core.js src/scene-store.js src/camera-core.js src/camera-store.js src/fusion-core.js src/multicamera-runtime.js src/environment-core.js src/environment-runtime.js src/environment-store.js src/scene-graph-core.js src/world-state-core.js src/perception-replay-core.js dist/tracky-v1.3/src/
          cd dist
          zip -r ../tracky-v1.3-deploy.zip tracky-v1.3

      - name: Verify deploy ZIP
        shell: bash
        run: |
          set -euo pipefail
          unzip -t tracky-v1.3-deploy.zip
          unzip -l tracky-v1.3-deploy.zip | grep -F "tracky-v1.3/agent-eyes.js"
          unzip -l tracky-v1.3-deploy.zip | grep -F "tracky-v1.3/src/perception-core.js"
          unzip -l tracky-v1.3-deploy.zip | grep -F "tracky-v1.3/src/environment-core.js"
          unzip -l tracky-v1.3-deploy.zip | grep -F "tracky-v1.3/src/environment-runtime.js"
          unzip -l tracky-v1.3-deploy.zip | grep -F "tracky-v1.3/src/environment-store.js"
          unzip -l tracky-v1.3-deploy.zip | grep -F "tracky-v1.3/src/scene-graph-core.js"
          unzip -l tracky-v1.3-deploy.zip | grep -F "tracky-v1.3/src/world-state-core.js"
          unzip -l tracky-v1.3-deploy.zip | grep -F "tracky-v1.3/src/perception-replay-core.js"
          sha256sum tracky-v1.3-deploy.zip

      - name: Upload deploy ZIP
        uses: actions/upload-artifact@v4
        with:
          name: tracky-v1.3-deploy
          path: tracky-v1.3-deploy.zip
          if-no-files-found: error
          retention-days: 30
